import 'package:flutter/material.dart';

class sictWidget extends StatelessWidget {
  const sictWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
