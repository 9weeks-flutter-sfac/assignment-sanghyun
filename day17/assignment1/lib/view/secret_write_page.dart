import 'package:flutter/material.dart';

class SecretWritePage extends StatelessWidget {
  const SecretWritePage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
